<!-- index.ejs -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>TEST</title>
    <style>
      .druzyna{
        background-color: cadetblue;
        height: fit-content;
        width: fit-content;
        margin: 10px;
        float: left;
      }
      #boxbox{
        
      }
    </style>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  </head>
  <body style="background-color: aquamarine;">
  <div id="boxbox">

    <% for(var i = 0; i < teamdata.length; i++) {%>
      <div class="druzyna">
        <p>Nazwa drużyny: <%= teamdata[i].teamName %> || 
        Skrót drużyny: <%= teamdata[i].teamShortName %></p>

        <p>Kapitan NICK: <%= teamdata[i].kapitanName %> ||
        Kapitan LINK OP.GG: <%= teamdata[i].kapitanLink %> ||
        Kapitan Pozycja: <%= teamdata[i].kapitanPozycja %></p>

        <p>Drugi NICK: <%= teamdata[i].s2ndName %> ||
        Drugi LINK OP.GG: <%= teamdata[i].s2ndLink %> ||
        Drugi Pozycja: <%= teamdata[i].s2ndPozycja %></p>

        <p>Trzeci NICK: <%= teamdata[i].s3rdName %> ||
        Trzeci LINK OP.GG: <%= teamdata[i].s3rdLink %> ||
        Trzeci Pozycja: <%= teamdata[i].s3rdPozycja %></p>

        <p>Czwarty NICK: <%= teamdata[i].s4thName %> ||
        Czwarty LINK OP.GG: <%= teamdata[i].s4thLink %> ||
        Czwarty Pozycja: <%= teamdata[i].s4thPozycja %></p>

        <p>Piąty NICK: <%= teamdata[i].s5thName %> ||
        Piąty LINK OP.GG: <%= teamdata[i].s5thLink %> ||
        Piąty Pozycja: <%= teamdata[i].s5thPozycja %></p>

        <p>Rezerwa NICK: <%= teamdata[i].rezerwaName %> ||
        Rezerwa LINK OP.GG: <%= teamdata[i].rezerwaLink %> ||
        Rezerwa Pozycja: <%= teamdata[i].rezerwaPozycja %></p>
        <button value="<%= teamdata[i]._id %>">confirm</button> <button value="<%= teamdata[i]._id %>">delete</button>
    </div>
    <% } %>
  </div>

  <script>
    $(document).ready(function(){
      $('button').click(function(){
        var button = $(this).val()
        var decs = $(this).text()
        if(decs == 'confirm'){
          //confirm
        }else{
          //send DELETE
          var xhttp = new XMLHttpRequest();
          xhttp.open('DELETE', '/dashboard/teamy/delete/'+ button, false)
          xhttp.set('Content-type', 'application/json')
          xhttp.send();
        }
      })
    })
  </script>
  </body>
</html>